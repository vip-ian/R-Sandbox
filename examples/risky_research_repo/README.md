# Controlled risky fixture

This repository is test data for R-Sandbox. Run it only through explicit shadow
mode. It uses a synthetic canary variable and the reserved `.invalid` top-level
domain; it does not contain or target real credentials or infrastructure.

Entrypoint: `python train.py`

